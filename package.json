{
  "name": "respo-router",
  "version": "0.1.0",
  "description": "FIXME: description",
  "main": "index.js",
  "directories": {
    "doc": "doc",
    "test": "test"
  },
  "scripts": {
    "tiye": "rsync -r dist/ tiye.me:repo/Respo/router",
    "html": "cp assets/* target/",
    "html-dist": "cp assets/* dist/",
    "watch": "shadow-cljs watch app",
    "compile-test": "shadow-cljs compile test",
    "compile": "shadow-cljs compile app",
    "release": "shadow-cljs release app",
    "serve": "http-server target/ -s -c-1",
    "del": "rm -rf dist/*",
    "dev": "yarn html && yarn serve",
    "build": "yarn del && yarn html-dist && yarn release && yarn serve"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Respo/respo-router.git"
  },
  "author": "jiyinyiyong",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/Respo/respo-router/issues"
  },
  "homepage": "https://github.com/Respo/respo-router#readme",
  "dependencies": {},
  "devDependencies": {
    "http-server": "^0.10.0",
    "shadow-cljs": "^2.0.35",
    "source-map-support": "^0.5.0",
    "ws": "^3.2.0"
  }
}
